
require("which-key").setup {
  preset = "classic",
  plugins = {
      marks = true,
      spelling = { enabled = false },
      presets = {
          operators = false,
      },
  },
  icons = {
    mappings = false,
  },
}
